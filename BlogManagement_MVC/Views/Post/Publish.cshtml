@model PostVM

<div class="container">
    <h3>Are you sure you want to publish this post?</h3>
    <div>

        <hr />
        <div>
            <h6>@Html.DisplayNameFor(model => model.Title)</h6>
            @Model.Title
        </div>
        <div>
            <h6>@Html.DisplayNameFor(model => model.Summary)</h6>
            @Model.Summary
        </div>
        <div>
            <h6>@Html.DisplayNameFor(model => model.CreatedAt)</h6>
            @Model.CreatedAt
        </div>

        <form asp-action="Publish">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Publish" class="btn btn-danger" /> |
            <input type="button" value="Cancel" onclick="location.href='@Url.Action("GetPostByUserId","Post")'" />
        </form>
    </div>
</div>